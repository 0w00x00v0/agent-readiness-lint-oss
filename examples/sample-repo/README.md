# widget-service (fictional sample)

A tiny, fictional Python service used as committed example input for
`agent-readiness-lint`. It is intentionally incomplete so the linter has
something to report.

This repository contains no real code, data, secrets, or credentials.

## What it "does"

Returns a greeting for a widget. That's it.

## Run the (pretend) app

```bash
python -m src.app
```
